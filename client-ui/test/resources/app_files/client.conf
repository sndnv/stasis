stasis {
  client {
    ops {
      backup {
        rules-file = "client.rules"
      }

      scheduling {
        schedules-file = "client.schedules"
      }
    }
  }
}

a {
  b {
    c = test-value
  }
}