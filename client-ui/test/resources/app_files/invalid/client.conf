stasis {
  client {
    ops {
      backup {
        rules-file = "missing.rules"
      }

      scheduling {
        schedules-file = "missing.schedules"
      }
    }
  }
}

a {
  b {
    c = test-value
  }
}